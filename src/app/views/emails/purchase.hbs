<html>

<head>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
        }
    </style>
</head>

<body>
    <strong>Olá, {{ ad.author.name }}</strong>
    <p>Voce tem uma nova solicitacao para anuncio: {{ ad.title }}</p>
    <br>
    <strong>{{ user.name }} ({{ user.email }}): </strong>
    <p>{{ content }}</p>

</body>

</html>
